<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

        <!-- sale order -->
        <record id="view_order_form" model="ir.ui.view">
            <field name="name">sale.order.form</field>
            <field name="model">sale.order</field>
            <field name="type">form</field>
            <field name="priority" eval="100" />
            <field name="inherit_id" ref="sale.view_order_form" />
            <field name="arch" type="xml">
                <data>
                    <xpath expr="//form[@string='Sales Order']//tree[@string='Sales Order Lines']//field[@name='name']" position="before">
                        <field name="line_number" />
                    </xpath>
                </data>
            </field>
        </record>

        <!-- purchase order -->
        <record id="purchase_order_form" model="ir.ui.view">
            <field name="name">purchase.order.form</field>
            <field name="model">purchase.order</field>
            <field name="type">form</field>
            <field name="priority" eval="100" />
            <field name="inherit_id" ref="purchase.purchase_order_form" />
            <field name="arch" type="xml">
                <data>
                    <xpath expr="//form[@string='Purchase Order']//tree[@string='Purchase Order Lines']//field[@name='date_planned']" position="before">
                        <field name="line_number" />
                    </xpath>
                </data>
            </field>
        </record>

    </data>
</openerp>
