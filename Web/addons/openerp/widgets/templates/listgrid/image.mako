    <div align="center">
      % if img:
        <img
            border="1"
            src="${img}"
            % if width:
                width="${width}"
            % endif
            % if height:
                height="${height}"
            % endif
        ></img>
     % endif
    </div>

