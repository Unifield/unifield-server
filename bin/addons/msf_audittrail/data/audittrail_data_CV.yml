-
  For Commitment Vouchers (account.commitment), track the creation, modification, and deletion
-  
  !python {model: audittrail.rule}: |
    name = 'Commitment Vouchers'
    object_ids = self.pool.get('ir.model').search(cr, uid, [('model', '=', 'account.commitment')], context=context)
    rule_id = self.search(cr, uid, [('object_id', 'in', object_ids)], context=context)
    if object_ids:
        fields = ['name', 'state', 'total']
        fields_ids = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=', 'account.commitment'), ('name', 'in', fields)], context=context)

        # For the Sequence
        field_other_name = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=', 'account.commitment'), ('name', '=', 'name')], context=context)
        field_other_id = False
        if field_other_name:
            field_other_id = field_other_name[0]

        vals = {
            'name': name,
            'object_id': object_ids[0],
            'log_write': True,
            'log_unlink': True,
            'log_create': True,
            'field_ids': [(6, 0, fields_ids)],
            'field_other_id': field_other_id,
        }

        if not rule_id:
            rule_id = self.create(cr, uid, vals, context=context)
        elif rule_id:
            self.write(cr, uid, rule_id, vals, context=context)
        # Subscribe to the rule
        self.subscribe(cr, uid, rule_id)
-
  For Commitment Voucher Lines (account.commitment.line), track the creation, modification, and deletion
-
  !python {model: audittrail.rule}: |
    name = 'Commitment Voucher Lines'
    object_ids = self.pool.get('ir.model').search(cr, uid, [('model', '=', 'account.commitment.line')], context=context)
    rule_id = self.search(cr, uid, [('object_id', 'in', object_ids)], context=context)
    if object_ids:
        fields = ['account_id', 'initial_amount', 'amount']
        fields_ids = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=' , 'account.commitment.line'), ('name', 'in', fields)], context=context)
        field_name = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=', 'account.commitment.line'), ('name', '=', 'account_id')], context=context)
        field_parent = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=', 'account.commitment.line'), ('name', '=', 'commit_id')], context=context)

        name_id = False
        parent_id = False

        if field_parent:
            parent_id = field_parent[0]
            if field_name:
                name_id = field_name[0]

        # For the Sequence
        field_other_name = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=', 'account.commitment.line'), ('name', '=', 'commit_number')], context=context)
        field_other_id = False
        if field_other_name:
            field_other_id = field_other_name[0]

        vals = {
            'name': name,
            'object_id': object_ids[0],
            'log_write': True,
            'log_unlink': True,
            'log_create': True,
            'field_ids': [(6, 0, fields_ids)],
            'parent_field_id': parent_id,
            'name_get_field_id': name_id,
            'field_other_id': field_other_id,
        }

        if not rule_id:
            rule_id = self.create(cr, uid, vals, context=context)
        elif rule_id:
            self.write(cr, uid, rule_id, vals, context=context)
        # Subscribe to the rule
        self.subscribe(cr, uid, rule_id)
