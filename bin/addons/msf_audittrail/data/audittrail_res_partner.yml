-
  For Partner, rule for the CREATE of objects with the list of fields to track
-
  !python {model: audittrail.rule}: |
    name = 'PARTNER CREATE WRITE'
    object_ids = self.pool.get('ir.model').search(cr, uid, [('model', '=', 'res.partner')], context=context)
    rule_id = self.search(cr, uid, [('name', '=', name)], context=context)
    if object_ids:
        fields = ['active', 'comment', 'credit_limit', 'customer', 'customer_lt', 'lang', 'manufacturer', 'name', 'partner_type',
                  'po_by_project', 'procurement_lt', 'property_account_payable', 'property_account_position', 'property_account_receivable',
                  'property_payment_term', 'property_product_pricelist', 'property_product_pricelist_purchase', 'property_stock_customer',
                  'property_stock_supplier', 'ref', 'split_po', 'supplier', 'supplier_lt', 'title', 'transport_0', 'transport_0_lt',
                  'transport_1', 'transport_1_lt', 'transport_2', 'transport_2_lt', 'transporter', 'zone', 'legal_name', 'state']
        fields_ids = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=' ,'res.partner'), ('name', 'in', fields)], context=context)
        vals = {'name': name,
                'object_id': object_ids[0],
                'log_create': True,
                'log_write': True,
                'log_unlink': False,
                'field_ids': [(6, 0, fields_ids)],
                }
        if not rule_id:
            rule_id = self.create(cr, uid, vals, context=context)
        elif rule_id:
            self.write(cr, uid, rule_id, vals, context=context)
        # Subscribe to the rule
        self.subscribe(cr, uid, rule_id)
-
  For PARTNER, rule for the DELETE of objects with the list of fields to track
-
  !python {model: audittrail.rule}: |
    name = 'PARTNER DELETE'
    object_ids = self.pool.get('ir.model').search(cr, uid, [('model', '=', 'res.partner')], context=context)
    rule_id = self.search(cr, uid, [('name', '=', name)], context=context)
    if object_ids:
        fields = ['name']
        fields_ids = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=' ,'res.partner'), ('name', 'in', fields)], context=context)
        vals = {'name': name,
                'object_id': object_ids[0],
                'log_create': False,
                'log_write': False,
                'log_unlink': True,
                'field_ids': [(6, 0, fields_ids)],
                }
        if not rule_id:
            rule_id = self.create(cr, uid, vals, context=context)
        elif rule_id:
            self.write(cr, uid, rule_id, vals, context=context)
        # Subscribe to the rule
        self.subscribe(cr, uid, rule_id)
-
  For Partner address, rule for the CREATE of objects with the list of fields to track
-
  !python {model: audittrail.rule}: |
    name = 'PARTNER ADDRESS CREATE WRITE'
    object_ids = self.pool.get('ir.model').search(cr, uid, [('model', '=', 'res.partner.address')], context=context)
    rule_id = self.search(cr, uid, [('name', '=', name)], context=context)
    if object_ids:
        fields = ['city', 'country_id', 'email', 'fax', 'function', 'mobile', 'name', 'phone', 'state_id', 'street', 'street2', 'title', 'type', 'zip']
        fields_ids = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=' ,'res.partner.address'), ('name', 'in', fields)], context=context)
        field_name = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=', 'res.partner.address'), ('name', '=', 'street')], context=context)
        field_parent = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=', 'res.partner.address'), ('name', '=', 'partner_id')], context=context)
        name_id = False
        parent_id = False
        if field_parent:
            parent_id = field_parent[0]
            if field_name:
                name_id = field_name[0]
        vals = {'name': name,
                'object_id': object_ids[0],
                'domain_filter': "[('partner_id', '!=', False)]",
                'log_create': True,
                'log_write': True,
                'log_unlink': False,
                'field_ids': [(6, 0, fields_ids)],
                'parent_field_id': parent_id,
                'name_get_field_id': name_id,
                }
        if not rule_id:
            rule_id = self.create(cr, uid, vals, context=context)
        elif rule_id:
            self.write(cr, uid, rule_id, vals, context=context)
        # Subscribe to the rule
        self.subscribe(cr, uid, rule_id)
-
  For PARTNER address, rule for the DELETE of objects with the list of fields to track
-
  !python {model: audittrail.rule}: |
    name = 'PARTNER ADDRESS DELETE'
    object_ids = self.pool.get('ir.model').search(cr, uid, [('model', '=', 'res.partner.address')], context=context)
    rule_id = self.search(cr, uid, [('name', '=', name)], context=context)
    if object_ids:
        fields = ['name']
        fields_ids = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=' ,'res.partner.address'), ('name', 'in', fields)], context=context)
        field_name = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=', 'res.partner.address'), ('name', '=', 'street')], context=context)
        field_parent = self.pool.get('ir.model.fields').search(cr, uid, [('model', '=', 'res.partner.address'), ('name', '=', 'partner_id')], context=context)
        name_id = False
        parent_id = False
        if field_parent:
            parent_id = field_parent[0]
            if field_name:
                name_id = field_name[0]
        vals = {'name': name,
                'object_id': object_ids[0],
                'log_create': False,
                'log_write': False,
                'log_unlink': True,
                'field_ids': [(6, 0, fields_ids)],
                'parent_field_id': parent_id,
                'name_get_field_id': name_id,
                }
        if not rule_id:
            rule_id = self.create(cr, uid, vals, context=context)
        elif rule_id:
            self.write(cr, uid, rule_id, vals, context=context)
        # Subscribe to the rule
        self.subscribe(cr, uid, rule_id)
